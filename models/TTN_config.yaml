model: 'STN'
data_name: 'clickbait' # clickbait, rotten-tomatoes, protein-binding
use_jit: True
post_sel: True
use_grad_clip: True
use_optax_reg: True
grad_clip: 100.0
n_epochs: 25
batch_size: 64
init_val: 0.01
ansatz: 'A14'
n_qubits: 1 # number of qubits per word
parse_type: 'unibox'  # unibox / height
n_layers: 1 
lr: 0.001